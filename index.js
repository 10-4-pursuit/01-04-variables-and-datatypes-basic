let age = 30;

const name = `Darnisha`;

let isRaining = false;

let numbers = [0, 2, 4, 6, 8];

let person = {
    firstName: `Lanell`,
    lastName: `Kerry`
};

console.log(age, name, isRaining)
console.log(numbers)
console.log(person)